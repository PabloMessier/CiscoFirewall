---
# Ansible configuration for Cisco ASA network device management
[defaults]
inventory = inventory.ini
host_key_checking = False
timeout = 60
# Increase for slow-booting ASAv appliances
command_timeout = 120

[persistent_connection]
connect_timeout = 60
command_timeout = 120
